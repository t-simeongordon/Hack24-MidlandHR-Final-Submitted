<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tsg.jsonextractiontry1">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SelectionPage">

        </activity>

        <activity android:name=".DisplayMain">

        </activity>

        <activity android:name=".MainActivity">

        </activity>

        <activity android:name=".SkillPage">

        </activity>

        <activity android:name=".SingleCustomLayout">

        </activity>

        <activity android:name=".SkillResult">

        </activity>

<activity android:name=".LocationPage">

</activity>

        <activity android:name=".LocationResult">

        </activity>

    </application>

</manifest>
